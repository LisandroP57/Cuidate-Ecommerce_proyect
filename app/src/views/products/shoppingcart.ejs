<!DOCTYPE html>
<html lang="es" class="html-cart">
    <%- include ('../partials/head', {title: 'Mi carrito'}) %>
    <body class="body-cart">
        <%- include ('../partials/header') %>

        <div class="container cf">
            <h1 class="mytitle"><span>Cuidate!</span>Seguir comprando</h1>
            <h1>Mi Carrito</h1>
            <% if (products.length === 0) { %>
              <div class="products-contain">
                <form class="register-form">
                    <h2 class="title">Actualmente no hay productos en su carrito!</h2>
                    <h4 class="subtitle">
                        Elije alguna de nuestras categorías para encontrar los
                        mejores productos:
                    </h4>
                    <section class="section-categories">
                      <div class="title_category">
                        <a href="/products/category/1">
                          <h2>Defensa personal</h2>
                        </a>
                      </div>
                      <div class="title_category">
                        <a href="/products/category/3">
                          <h2>KIDS</h2>
                        </a>
                      </div>
                      <div class="title_category">
                        <a href="/products/category/2">
                          <h2>Accesorios+Combos</h2>
                        </a>
                      </div>
                    </section>
                </form>
              </div>
            <% } else { %>

            <div class="continue cf">
                <a href="#" class="continue" onclick="clearCart('<%= cart.id %>')">Vaciar mi carrito</a>
            </div>
            <form action="">
                <div class="cart">
                    <ul class="tableHead">
                        <li class="prodHeader">Producto</li>
                        <li>Total</li>
                        <li class="delete">Borrar</li>
                    </ul>
                    <ul class="cartWrap">
                      <li class="items odd">
                          <% products.forEach(item => { %>
                            <div class="infoWrap">
                                <div class="cartSection">
                                    <img class="itemImg" src="/images/products/<%- item.product.images[0] ? item.product.images[0].image : '' %>" alt="">
                                    
                                    <h3><%- item.product.name %></h3>
                                    <p>
                                        <button type="button" class="btn-rest" onclick="removeOneProduct('<%- item.id %>')">
                                            -
                                        </button>
                                        <input type="text" class="qty" placeholder="<%- item.quantity %>"/>
                                        <button type="button" class="btn-add" id="btn-add" onclick="addToCart('<%= item.product.id %>')">
                                            +
                                        </button>
                                        x $<%- item.product.price %>
                                        
                                    </p>
                                </div>
                                <div class="prodTotal cartSection">
                                    <p>$<%- (Number(item.product.price) * Number(item.quantity))%></p>
                                </div>
                                <div class="cartSection removeWrap">
                                    <button class="remove" onclick="removeAllOfOneProduct('<%- item.id %>')">
                                        x
                                    </button>
                                </div>
                            </div>
                            <br>
                            <% }) %>
                        </li>
                    </ul>
                </div>

                <div class="promoCode">
                    <label for="promo">Tenes un cupon de descuento?</label>
                    <input type="text" placeholder="Ingresalo!"/>
                    <a href="#" class="btn"></a>
                </div>

                <div class="subtotal cf">
                    <ul>
                        <li class="totalRow">
                            <span class="label">Subtotal</span
                            ><span class="value">$3500.00</span>
                        </li>

                        <li class="totalRow">
                            <span class="label">Envio</span
                            ><span class="value">$1500</span>
                        </li>
                        <li class="totalRow final">
                            <span class="label">Total</span
                            ><span class="value">$5500.00</span>
                        </li>
                        <li class="totalRow">
                            <a href="#" class="btn continue">Verificar</a>
                        </li>
                    </ul>
                </div>
            </form>
            <% } %>
            
        </div>
        <script src="/js/cart.js"></script>
        <%- include ('../partials/footer') %>
    </body>
</html>